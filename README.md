minimal config to start a project

How to use:

1. Get the whole repository

```sh
npx degit bodadotsh/config new_project
```

2. Pick and select files:

```sh
curl -O https://raw.githubusercontent.com/bodadotsh/config/refs/heads/main/biome.json
```
